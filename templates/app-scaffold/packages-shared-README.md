# Shared package (`packages/shared`)

Optional shared TypeScript types, validation schemas, or constants used by `apps/web` and `apps/api`.

Add a real package manager workspace when you introduce code here.
