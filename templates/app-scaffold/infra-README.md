# Infra (`infra/`)

Database migrations, Docker Compose, deployment manifests, runbooks.

**Rule of thumb:** no secrets in git — reference env vars and use `local/` or your host’s secret store for values.
