# Web app (`apps/web`)

Browser UI for this product. Scaffold placeholder — replace with Vite, Next.js, SvelteKit, etc.

**Contracts:** Consume `apps/api` (and public env only; no secrets in the client bundle).
